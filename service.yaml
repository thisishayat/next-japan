apiVersion: v1
kind: Service
metadata:
  name: nextjapan-service
  labels:
    app: nextjapan
spec:
  type: NodePort
  selector:
    app: nextjapan
  ports:
    - name: http
      port: 80
      targetPort: 3000
      nodePort: 30080
